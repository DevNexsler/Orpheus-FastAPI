# Supabase Configuration
# Copy this file to .env and fill in your Supabase credentials

# Supabase project URL (found in your Supabase dashboard)
SUPABASE_URL=your_supabase_url

# Supabase service role key (found in your Supabase dashboard under API settings)
# It's recommended to use the service_role key for server-side applications
SUPABASE_KEY=your_supabase_key

# Bucket name for storing audio files (will be created if it doesn't exist)
SUPABASE_BUCKET=tts-audio 